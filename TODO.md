Responsive
Common words dict, regardless of language?
Add Visit history triggered when a user views a profile
Add a sort by age, location, fame and common tags to the home page
Add who has viewed a users profile
Add who liked a user
Add public fame rating (the formula is up to us)
Implement GPS map update when values changed manually
Give priority to close people first
Create connexions
Remove likes that will break connections
See if user is connected or not
Report fake accounts
Can't like if no photo
Add 5 photos upload
Block users
From the profile page:
    If it's personal page, See likes and views
    If it's other person page, be able to unlike and disconnect
Chat: If two users have connected, allow chatting, notify user from new messages
RT Notifications (10 sec delay) with history when:
    Receiving a like
    Profile is viewed
    Receives a new message
    A user likes them back (new connection)
    A connected user unlikes them :C

BUG:
When updating manually location (lat, lon) it doesn't save with the gps disabled

WHAT?:
Users must also have the option to modify their location in their profile
at any time
Profiles should display all available information except for the email address and pass-
word...